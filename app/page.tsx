import SearchBar from "@/components/search-bar"

export default async function Home() {
  return (
    <div className="w-full h-64 flex items-center justify-center bg-center bg-local bg-[url(/1267712.png)] bg-no-repeat">
      <SearchBar />
    </div>
  )
}
